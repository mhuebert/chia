Canonical metadata for projects is stored in root-level `projects.edn`.

Generate pom.xml for root-level project:

```
clj -m build pom
```

Watch the demo build:

```
shadow-cljs clj-run demo/watch
```